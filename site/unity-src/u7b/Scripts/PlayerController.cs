using UnityEngine;

public class PlayerController : MonoBehaviour
{
    public int Hunger;
    public int Hygiene;
    public int Enervation;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
